server:
  port: 8082
spring:
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_ROOT_PATH}
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
# management.endpoints.web.exposure.include=*
# spring.cloud.config.server.git.uri=${CONFIG_ROOT_PATH}
# spring.cloud.config.server.git.repos=master
